# v1.4.6 검수 화면 폭과 비고 비율 정정

v1.4.5에서 검수 화면을 무제한으로 넓히고 비고가 남는 너비를 모두 차지하도록 한 판단이
사용자의 의도와 달랐다. 기존 1080px보다 조금 넓은 최대 1200px 중앙 정렬로 조정하고,
비고 열은 약 180px로 지정한다. 해당 칸 아래 현재/입력 세로 비교와 입력·선택 동작은 유지한다.

사용자 화면 정정 계약 → #48 R20/R21/R22 → WP08/09 → 본 PR/source/CI.
운영 .env 없는 격리 worktree와 합성 자료로 검사하며 운영 초안·실제 금액은 건드리지 않는다.

- 1280/1920px에서 중앙 정렬·최대 폭·비고 너비를 확인하도록 기존 E2E를 수정했다.
  기존의 화면 90% 채우기 검사는 잘못된 디자인 목표였으므로 삭제했다.
- 독립 CSS 검토에서 추가 결함 없음. 좁은 화면의 표 안 가로 이동과 집중보기를 유지한다.
- 구버전 Chromium E2E 18건 통과(55.00초), 버전 검사 3건 통과.
- 1280/1920px 합성 화면을 직접 확인해 중앙 여백과 비고 비율을 검토했다.
- Ruff check/format, mypy app scripts 통과. 필수 CI 결과는 PR merge ref로 연결한다.

Win7 Chrome 109·갤럭시 실기 검증은 미실행이다. 릴리스와 실제 배포 결과는 source·CI·이미지
정보와 함께 릴리스에 기록하고, 작업용 파일을 정리할 때 운영 자료와 기존 제공 Excel은 보존한다.
