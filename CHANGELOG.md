# Changelog

버전별 주요 변경 사항을 기록합니다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따릅니다.

## [Unreleased]

## [1.5.1] - 2026-09-09

- 비재직 잔액을 입력 당시 인원·포인트번호·이름·개인번호·유형·월·재직 구분에 연결해, 번호 재사용 후 초안 복원 시 다른 인원에게 잔액이 적용되는 경로 차단
- 연결 정보가 없는 기존 초안이나 연결이 달라진 잔액은 이전 입력으로 별도 보존하고 재입력 요구. 잘못된 입력 원문과 다른 초안 내용 유지
- 월간 작업의 원본 정정판으로 재직 기간을 연결해 번호 재사용·과거 삽입 후 화면별 기간 불일치 수정. 최근 기간 미확인 시에도 확인된 과거 기간 보존
- 재직 팀 통계에 월간 활동과 보유 잔액 각각의 당시 상태 적용, 유형 미확인 계정에도 팀 필터 적용. Excel 같은 기준 유지
- 금액 오류 안내에서 실제 오류 입력칸으로 이동하고 집중 보기·필터로 숨겨진 칸도 표시
- 변경 이력 목록·상세 시각을 한국 시간으로 통일하고 변경 전후 금액에 천 단위 쉼표 적용. UTC 저장 시각·원문·식별자는 유지
- 자동 저장의 불필요한 동일 URL 갱신을 제거하고 E2E가 실제 재검수 화면 전환 완료를 기다리도록 보완
- 월간 요청·초안·CSRF 파서의 폼 한도를 통일해 대규모 요청과 비재직 연결 정보·보존 입력 함께 처리

## [1.5.0] - 2026-09-09

- 월간 검수 열 제목 고정, 이월 잔액→충전액 순서와 파랑·주황 구분, 숫자만 표시하는 직전 잔액 안내(10,000,000 표시 너비 기준)
- 금액 입력 중 천 단위 쉼표 자동 표시와 확정 버튼 근처의 실패 원인·해결 방법 안내
- 기존 비재직 일반 인원도 월간 잔액을 필수 입력하고 초안으로 보관. 비재직 상태·0원 충전을 유지하며 신규 전환 인원에 중복 집계하지 않음
- 팀 목록·상세의 일반 인원 재직/비재직별 현재 총 잔액 합계와 색상 선택 자동 저장
- 인원 목록·팀 상세·개인 상세에 장부 기준 재직 기간을 최신순으로 표시. 복귀 구간을 나누고 기록 공백은 비재직 근거가 없으면 연결
- 대시보드에 재직 팀 통계와 이전 확인 잔액을 포함한 전체 팀 통계를 제공하고 Excel에도 같은 두 표 제공
- 기존 초안·장부·Excel v1/v2 입력 호환 유지. DB 스키마 변경 없음

## [1.4.6] - 2026-09-08

- 검수 화면을 최대 1200px의 중앙 정렬 레이아웃으로 조정해 기존보다 조금 넓은 화면과 좌우 여백 확보
- 비고 열 너비를 지정하여 큰 모니터에서도 과도하게 늘어나지 않도록 수정

## [1.4.5] - 2026-09-08

- 월간 검수 화면이 브라우저 폭을 넓게 사용하도록 조정하고 열별 너비를 내용에 맞춰 포인트번호·금액 확인 개선
- 연결 상태 태그를 이름 아래에 표시하고 현재 등록값·이번 입력값 선택 버튼을 해당 칸 아래 위아래 두 줄로 배치
- 좁은 화면의 표 안 가로 이동과 기존 연결·선택 후 위치 복원 유지

## [1.4.4] - 2026-09-08

- 월간 검수의 자동 연결·직접 연결·연결 확인·신규 상태를 태그와 색상으로 구분하고 업무 순서에 맞춰 화면 열 재배치
- 수동 연결 시 요청서 정보를 덮어쓰지 않고 현재 값과 이번 입력값의 차이를 나란히 표시해 항목별 선택 지원
- 자동 연결된 행의 팀·계급 등 차이에도 같은 선택 절차 적용. 선택 결과는 초안에 보존하고 확정 때 현재 인원 마스터에 반영
- 이번 입력의 빈 팀·계급을 명시적으로 선택하면 해당 현재 값을 비울 수 있으며 계정 유형 변경은 기존 인원 편집에서 처리
- 신규를 명시적으로 구분하되 외부 발급 포인트번호가 없으면 초안 유지. 이미 등록된 번호를 신규로 확정하는 경로 차단
- 재검수·연결 뒤 스크롤과 입력 위치 유지. Excel v2 열 순서와 기존 v1 호환 유지

## [1.4.3] - 2026-09-08

- 번호 없는 요청서에서 이름·개인번호·계정 유형이 모두 정확히 일치하는 기존 계정이 하나면 자동 연결
- 여러 계정이 일치하거나 공용계정의 개인번호가 비어 있으면 자동 연결하지 않고 수동 선택 유지
- 자동 연결은 요청서의 팀·계급·충전액·비고·이월을 보존하고 포인트번호만 보완. 수동 연결 변경 지원
- 연결 대상이나 이름·개인번호 변경 시 기존 이월을 다시 확인하며, 수동 연결은 기존의 현재 마스터 보완·이월 초기화 유지
- Excel v2 양식과 기존 v1 업로드 호환 유지

## [1.4.2] - 2026-09-08

- 포인트번호 없는 월간 요청서에서 이름 또는 개인번호가 일치하는 기존 인원 후보를 관리자가 선택해 연결
- 연결 시 현재 인원 정보를 보완하고 충전액·비고·원문을 보존하며 이월 잔액은 다시 확인
- 표준 Excel v2의 포인트번호 열 제거와 제목 포함 번호 없는 붙여넣기 지원. 기존 v1 및 번호 포함 붙여넣기 호환 유지
- 포인트번호는 외부 시스템 발급값만 사용. 신규 번호 미발급 행은 초안으로 보존하고 발급 후 입력·재검수하여 확정
- 대규모 이관 장부 조회에서 동일 관측의 중복 복사·프로필 보완을 생략


## [1.4.1] - 2026-09-07

월간 처리 인원 집계, 과거 월 추가 시 재직 판정과 여러 줄 비고 보존 오류 수정.

### 변경

- 개별 등록·복귀 후 월간 요청서에서 빠진 인원도 실제 비재직 전환으로 집계
- 비어 있던 과거 월에 추가한 일반 인원 기록은 충전액 0원이면 비재직, 지급액이 있으면 재직으로 표시
- Excel 업로드·검수·초안·금액 수정·보고서 다운로드에서 비고의 줄바꿈과 빈 줄 보존

### 안전성

- 과거 월 기록의 재직 판정이 현재 재직 상태를 바꾸지 않으며 공용계정은 항상 재직 유지
- 이름·번호·팀·계급은 현재 인원 정보로 조회하고 기존 장부 금액과 변경 이력 보존

## [1.4.0] - 2026-09-07

월간 처리의 입력 검증·초안·Excel 기능과 금액 수정·백업 안전성 개선.

### 추가

- 월간 요청서의 서버 초안 저장·자동 저장·중단 후 복구와 기기 간 저장 충돌 안내
- 표준 Excel 요청서 가져오기와 월별·개인별·팀별 보고서 다운로드
- 과거 월 금액 수정, 현재 잔액 수정, 수정 전후 값과 변경 사유 이력
- 장부 정합성 검사와 백업·복원·AI 설정 상태 확인

### 변경

- 월·금액·포인트번호 입력 검증을 강화하고 오류가 있는 행과 입력 원문을 보존
- 검수 후 인원·금액·처리 월이 달라지면 변경 내용을 다시 검토하도록 처리
- 검수 화면에 집중 입력, 미입력 행 이동, 상태·변경별 필터와 정렬 제공
- 백업·복원·배포의 환경설정과 데이터 경로를 통일하고 정기 백업 실행 지원

### 안전성

- 기존 인원·월별 지급액·잔액을 보존하고 기존 장부의 지급 이력에 따른 재직·비재직 기준 유지
- 금액 수정 시 다음 실제 기록의 순사용과 현재 잔액을 함께 계산하고 중복·오래된 제출 차단
- SQLite 백업 검증, 복원·마이그레이션 예행 검증과 이전 전후 데이터 대사
- CSRF 방어, 비밀번호 변경 후 기존 세션 폐기, 외래키·입력 제약과 운영 설정 검증 강화
- Gemini 요청의 크기·동시 실행·대기 시간을 제한하고 실패 시 입력 보존 및 민감정보 노출 방지

## [1.3.0] - 2026-08-23

PointBook 서버를 Codex 실행 세션과 분리하고 기존 SQLite 데이터를 그대로 보존하는 Docker Compose 운영 전환.

### 추가

- 단일 앱 컨테이너 운영용 `Dockerfile`과 `docker-compose.yml`
- 컨테이너 자동 재시작, localhost 전용 포트, healthcheck, 정상 종료 유예 설정
- 실제 DB·포트와 격리된 Compose 스모크 및 SQLite 재시작 영속성 검사

### 변경

- `scripts/run.sh`, `scripts/stop.sh`, `scripts/deploy.sh`를 Docker Compose 운영 흐름으로 전환
- CI 구버전 Chromium E2E 이후 운영 Compose 스모크 실행
- 서버 로그와 상태 확인을 Docker Compose 명령으로 통일

### 안전성

- `data/pointbook.db`와 백업은 호스트 bind mount에 유지하며 이미지에 포함하지 않음
- 배포 이미지를 서버 중지 전에 빌드하고, PointBook만 중지한 뒤 SQLite 사전 백업 생성
- 기존 PID 방식 서버는 명령줄과 작업 디렉터리가 현재 저장소와 일치할 때만 종료
- PostgreSQL 전환이나 데이터 변환 없이 기존 SQLite 스키마와 이력을 그대로 보존

## [1.2.3] - 2026-08-23

기존 팀의 소속 인원을 유지한 채 구분 색을 바로 바꿀 수 있도록 팀 관리 화면을 개선한 핫픽스.

### 추가

- 팀 목록의 각 팀에 기존 색을 바로 바꾸는 자유 색상 팔레트와 저장 기능 추가

### 안전성

- 팀 색상 변경 시에도 `#RRGGBB` 형식을 검증하고, 잘못된 값은 저장하지 않도록 차단
- 색상만 변경하며 기존 팀명·소속 인원·잔액·월별 이력은 유지

## [1.2.2] - 2026-08-23

인원과 팀원 현황을 원하는 기준으로 빠르게 비교할 수 있도록 목록 화면을 개선한 핫픽스.

### 추가

- 인원 관리 목록에 각 계정의 현재 총잔액 표시
- 인원 관리의 이름·포인트번호·개인번호·구분·팀·계급·상태·총잔액 서버 정렬
- 팀원 목록의 이름·포인트번호·개인번호·계급·상태·총잔액 서버 정렬

### 변경

- 인원과 팀원 목록의 기본 순서를 재직 우선, 같은 상태에서는 이름순으로 통일
- 인원 목록의 필터·검색·페이지 이동 시 선택한 정렬 조건 유지
- 팀 추가의 고정 색상 스와치를 브라우저 기본 자유 색상 팔레트로 교체
- 대시보드의 월간 인원 수를 전체 잔액 기록이 아닌 당월 재직자와 당월 신규 비재직 전환자만 집계하도록 수정

### 안전성

- 팀 색상을 `#RRGGBB` 형식으로 검증하고 잘못된 값을 저장하지 않도록 차단
- 밝은 팀 색상에서도 이름이 보이도록 배지 글자색을 배경 대비에 따라 자동 선택

## [1.2.1] - 2026-08-23

팀별 재직 현황과 잔액을 더 빠르게 파악할 수 있도록 팀 화면을 개선한 핫픽스.

### 변경

- 팀 목록에서 공용 계정을 제외한 전체·재직·비재직 인원수를 구분해 표시
- 팀 상세의 소속 인원을 재직 우선, 같은 상태에서는 이름순으로 정렬
- 팀원 목록에 현재 총잔액을 추가

## [1.2.0] - 2026-08-23

누적 포인트 장부 이관과 포인트번호 기반 계정 식별 체계 전환.

### 추가

- 8자리 `point_no` 필수·고유 식별자와 일반/공용 계정 구분
- 28개월 누적 장부 전용 마스킹 dry-run, 백업 게이트, 단일 트랜잭션 apply CLI
- 포인트번호 검색·표시·편집과 중복 개인번호 허용
- WSL 실서버용 `scripts/deploy.sh` 배포 스크립트

### 변경

- 월간 요청서·AI·엑셀 동기화를 포인트번호 기준으로 전환
- 사용량을 부호 있는 순사용으로 표시하고 적립·이전으로 인한 음수 값을 보존
- 공용 계정을 일반 재직자·팀·개인 통계와 자동 비재직 전환에서 제외
- 검수 화면의 이월 잔액을 편집 가능한 인원 행에 직접 연결하고, 누락된 잔액이 있으면 확정을 중단

### 안전성

- 기존 테스트 계정 교체는 월별 이력이 없고 전체 기존 계정이 미매칭 2개인 경우로 제한
- 배포 시 서버 종료를 확인한 뒤 SQLite DB를 백업하며, 종료 실패 시 배포를 중단
- E2E 컨테이너를 Python 3.10 기반으로 전환하고 임시 디렉터리 취약점이 수정된 pytest 9.1.1 적용

## [1.1.0] - 2026-08-13

보안·운영 하드닝, 데이터 무결성·성능 강화, Alembic 마이그레이션 도입, 신규 기능(백업·비밀번호 변경·페이지네이션).

### 추가

- **Alembic 마이그레이션**: ad-hoc `migrate()`를 Alembic으로 대체 (`migrations/`, `alembic.ini`). 기동 시 `upgrade head` 자동 실행, 기존 1.0.x DB는 `alembic_version` 부재 시 현재 상태를 head로 자동 표식(stamp). CI에 마이그레이션 검증(upgrade + check) job 추가
- **DB 자동 백업**: 월간 확정 커밋 전 `data/backups/`에 타임스탬프 복사본 자동 생성, 보관 개수 제한(`BACKUP_KEEP`, 기본 30). 수동 백업 스크립트 `scripts/backup.py`
- **설정 페이지**: `/settings`에서 관리자 비밀번호 변경 (현재 비밀번호 검증, 8자 이상, 확인 일치)
- **인원 목록 페이지네이션**: 페이지당 50명, 이전/다음 + 총 인원 표시
- **로깅**: 공통 로거(`app/logging.py`) — 시작·보안 경고·확정·백업·비밀번호 변경 이벤트 기록 (민감정보 미기록)

### 보안

- **시크릿 fail-fast**: `SECRET_KEY`/`ADMIN_PASSWORD` 기본값 사용 시 경고 로그, `ENFORCE_SECURE_DEFAULTS=true`면 서버 시작 거부
- **로그인 레이트리밋**: N회 실패 시 잠금(429), 성공 시 초기화 (`LOGIN_MAX_ATTEMPTS`/`LOGIN_LOCKOUT_SECONDS`)
- **업로드 검증**: 이미지 확장자·최대 크기(`MAX_UPLOAD_MB`) 제한
- **세션 쿠키 플래그**: `COOKIE_SAMESITE`(기본 lax)·`COOKIE_SECURE` 명시

### 수정

- **확정 트랜잭션 원자화**: 동기화·잔액 계산·스냅샷 저장을 단일 커밋으로 묶고, 실패 시 롤백 + 친절한 오류 페이지
- **개별 수정 불변식**: 인원 수정은 항상 현재 상태 + 최신 월 기록 1건만 변경(이전 월 기록 보존) 명시·회귀 테스트
- **버전 상수 분리**: `pyproject.toml` 읽기 제거 → `app/_version.py` 단일 소스 (wheel 배포 크래시 방지)

### 성능

- **`previous_total` N+1 제거**: 인원×월 이중 순회를 단일 조인 쿼리로 교체
- **stats eager loading**: `trend`/`team_summary`/`person_summary`에 `selectinload` 적용

### 수정 (파싱)

- 붙여넣기 파싱의 중복·데드 코드 제거, 탭 우선 구분자 처리 명시

## [1.0.0] - 2026-08-08

최초 정식 릴리스. 소방서 포인트 충전 요청서(엑셀)를 웹에서 관리하는 서비스의 첫 배포 버전.

### 추가

- **월간 요청서 처리 플로우**
  - 요청서 사진 업로드 → AI 테이블 인식 (Gemini 프로바이더, `GEMINI_MODEL` 설정) 또는 표 붙여넣기
  - 검수 화면: 인식 목록 수정(행 추가/삭제), DB 전체 대조 변경 예상(신규/복귀/유지·팀변경/비재직), 처리 대상 전체 인원의 이월 잔액 입력(직전 잔액 안내)
  - 확정 시 재직 상태 동기화 + 잔액 계산 + 월 스냅샷 저장 (단일 트랜잭션)
- **재직 상태 자동 동기화**: 요청서에 있으면 재직(복귀 포함), 없으면 비재직 처리, 신규는 재직 추가, 팀 변경 자동 반영 (신규 팀은 마스터에 자동 생성)
- **잔액 계산**: 매달 사용 합계 = 지난 달 총 잔액 − 이월 잔액, 총 잔액 = 당월 금액 + 이월 잔액. 비재직자 잔액 보존·복귀 시 이어짐
- **개별 금액 입력·수정**: 인원 추가/수정 화면에서 이월 잔액·당월 충전 금액 입력 (월간 처리 전에도 가능), 월간 기록이 있으면 사용 합계·총 잔액 자동 재계산
- **인원·팀 관리**: 개인번호+이름 고유값, 재직↔비재직 전환, 팀 마스터(색상 구분·상세 페이지), 인원 삭제 없음(비재직 처리로만 관리)
- **대시보드**: 월별 요약 카드(인원·충전·사용·잔액), 월별 추이 차트(Chart.js self-host), 팀별/개인별 통계(정렬 지원), 월 선택
- **엑셀 이관 스크립트**: 기존 요청서 엑셀 → DB 초기 이관 (헤더 별칭 인식, 빈 DB 전용)
- **인증**: 세션 쿠키 기반 단일 관리자 로그인
- **E2E 테스트**: 구버전 Chromium(≈Chrome 110) Playwright — Win7 마지막 Chrome(109)과 같은 Blink 세대 검증

### 수정

- **KST 시간대 버그**: UTC 기준이던 "이번 달" 계산을 한국 시간으로 수정 (월초 새벽 전 달로 잘못 잡히던 문제)
- **요청서 행 파싱 100명 하드 한도 제거**
- **인원 검색 422 오류**: 팀 필터 빈 값(전체 팀) 전송 시 파싱 실패 수정
- **스테일 페이지 문제**: HTML 응답에 `Cache-Control: no-store` 적용 (추가/수정 후 목록이 갱신 안 보이던 문제)
- **테이블 첫 행 가림 버그**: 스티키 테이블 헤더가 첫 번째 데이터 행을 덮던 CSS 버그 제거
- **Tailscale serve 포트 점유 대응**: run.sh에 HOST 바인딩 옵션과 포트 점유 원인 안내 추가
- **run.sh 백그라운드 안정화**: `uv run` 래퍼 대신 venv 파이썬 직접 실행 + 느린 시작 대기 루프

### 개선

- **디자인 리뉴얼**: 모던 블루 톤 (다크 네이비 탑바 + 블루 액센트), Pretendard self-host, 탑바 현재 페이지 강조, 색상 스와치 UI, 터치 타겟 44px
- **금액 수정 UI 통합**: 금액 수정은 인원 수정 페이지에서만, 상세 페이지는 표시 전용
- **검수 화면 UX**: 각 인원의 직전 총 잔액 안내, 확정 버튼 확인 다이얼로그
- **문서화**: 아키텍처 다이어그램(ERD·시퀀스·플로우), 사용 가이드(실사용 체크리스트·외부망 접속 안내)
- **CI**: ruff(린트/포맷) → mypy → pytest(커버리지 85%+) → pip-audit → gitleaks → E2E, squash 머지 워크플로

### 배포

- 서버: WSL 상시 구동 (`scripts/run.sh`, `HOST=127.0.0.1` 권장)
- 접속: 로컬 `localhost:8002`, Tailscale serve(내부)·Funnel(외부 공개), 갤럭시 실기기 대응
- DB: SQLite 단일 파일 (`data/pointbook.db`), 기동 시 스키마 자동 마이그레이션
